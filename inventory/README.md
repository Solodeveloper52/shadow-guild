# Inventory

Machine-readable catalog source of truth.

## Files

- `registry.json`: all catalog items, bundles, routing metadata, and install selectors.

## Notes

Installer, validators, and audit scripts depend on this file.
Treat updates as contract changes and run full validation after edits.
